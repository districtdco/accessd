// Package ssh implements the SSH proxy server.
// Clients authenticate via keyboard-interactive (session token challenge-response).
// Full session I/O is recorded in asciicast v2 format.
package ssh
